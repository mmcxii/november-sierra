// localStorage key the sign-up form writes a referral code to. Read by the
// onboarding theme step (see src/components/dashboard/onboarding-form) which
// applies it via redeemReferralCode after the user verifies their email and
// signs in for the first time.
export const REFERRAL_LOCALSTORAGE_KEY = "anchr_referral_code";
